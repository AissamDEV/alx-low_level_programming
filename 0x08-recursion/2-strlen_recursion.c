#include <string.h>
/**
  *_strlen_recursion- function that returns
  *the length of a string
  *@s: string
  *Return: length of s
  */
int _strlen_recursion(char *s)
{
	int length = strlen(s);

	return (length);
}
