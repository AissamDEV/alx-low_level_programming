#include <stdlib.h>
#include "main.h"
/**
  *_atio - function thats convert a string
  *to an integer
  *@s: pointer to the string to convert
  *Return: the converted int
  */
int _atio(char *s)
{
	int cn = atoi(s);

	return (cn);
}
